package com.beetech.mvcspringboot.constants;

/**
 * The enum Paypal config enum.
 */
public enum PaypalConfigEnum {
    /**
     * Mode paypal config enum.
     */
    MODE,
    /**
     * Client id paypal config enum.
     */
    CLIENT_ID,
    /**
     * Client secret paypal config enum.
     */
    CLIENT_SECRET
}
